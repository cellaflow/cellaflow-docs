---
title: "Architecture"
description: "How the CellaFlow engine, Python SDK, and RocksDB state store fit together."
icon: "layers"
---

# Architecture

CellaFlow has three layers that work together: the **Python SDK** (decorators + gRPC client), the **Rust Engine** (state machine + persistence), and **RocksDB** (durable storage).

---

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      Your Python Application                     │
│                                                                   │
│   @workflow          @step              @tool                     │
│   ─────────          ─────              ──────                   │
│   • starts session   • commits results  • idempotency check      │
│   • loads graph      • lease-free       • fencing token lease    │
│   • replay intercept                    • background heartbeat   │
│                                                                   │
│   ───────────────── gRPC (port 50051) ──────────────────────    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CellaFlow Rust Engine                          │
│                                                                   │
│   WorkflowEngineService        IdempotencyService                │
│   ─────────────────────        ──────────────────                │
│   • StartSession               • CheckIdempotency                │
│   • CommitStep                 • CommitIdempotencyKey            │
│   • GetSessionGraph            • AcquireLease / RenewLease       │
│   • RegisterWorkflow           • ReleaseLease                    │
│   • CancelSession                                                 │
│                                                                   │
│   ─────────────── RocksDB (embedded) ────────────────────────   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         RocksDB                                   │
│                                                                   │
│   sessions/          steps/              idempotency/            │
│   ─────────          ──────              ─────────────           │
│   • session metadata  • append-only      • key → result          │
│   • workflow version    event graph       • fencing tokens        │
│   • session state      • MessagePack      • lease expiry          │
└─────────────────────────────────────────────────────────────────┘
```

---

## The Transparent Replay Flow

When a workflow is resumed after a crash, the SDK replays committed steps from the event graph before resuming live execution:

```
@workflow(version="1.0.0")
def my_workflow(topic: str) -> str:
    data   = search_web(topic)       # seq:1
    result = analyze(data)           # seq:2 ← crashed here
    report = format_report(result)   # seq:3 ← not yet committed
    return report
```

**On restart with session ID:**

```
1. SDK calls GetSessionGraph → receives [seq:1 ✅, seq:2 ✅]
2. search_web() intercepted → CACHED (0ms) → returns stored result
3. analyze()    intercepted → CACHED (0ms) → returns stored result
4. format_report()          → LIVE → executes → CommitStep(seq:3)
5. Workflow completes
```

Zero re-execution of completed steps. Zero re-spent API tokens.

---

## Engine Boot Sequence

The engine follows a strict ordered startup. Every step must succeed before the next begins:

| Step | Action | On Failure |
|:---|:---|:---|
| 1 | Load config from environment variables | — |
| 2 | Initialize OTLP telemetry (traces + metrics → `localhost:4317`) | Degrades gracefully |
| 3 | Initialize structured JSON logger | — |
| 4 | Resolve socket bind address | Exit with error |
| 5 | Register OS signal handlers (SIGINT, SIGTERM) | Exit with error |
| 6 | Initialize RocksDB storage backend | `exit(1)` |
| 7 | Create database failure watchdog | — |
| 8 | Spawn health server (`/health/ready` → 503 until ready) | Continues with warning |
| 9 | Instantiate gRPC engine service | — |
| 10 | **Boot recovery** — scan sessions, reconstruct state, respawn timers | Exit with error |
| 11 | Set `recovery_complete = true` → `/health/ready` returns 200 | — |
| 12 | Configure auth interceptor (Bearer / `x-api-key`) | — |
| 13 | Configure TLS if cert + key provided | Exit with error |
| 14 | Register gRPC reflection services | Exit with error |
| 15 | Start Tonic gRPC server | — |

<Warning>
Step 10 (boot recovery) is intentionally strict — the engine will not start with a partial view of durable state. Any deserialization failure is treated as fatal.
</Warning>

---

## Serialization: MessagePack

All step payloads — inputs, outputs, and graph records — are serialized with **MessagePack** rather than JSON or pickle. Reasons:

- **Performance** — binary format, significantly smaller than JSON for numeric/byte data
- **Security** — no remote code execution deserialization vectors (unlike pickle)
- **Cross-language** — same binary representation across Python, Go, Rust, TypeScript

The Python SDK automatically serializes and deserializes all step payloads. You pass and receive normal Python dicts, lists, and primitives.

---

## gRPC Transport

The engine exposes a single gRPC endpoint on port `50051` with TLS and authentication support:

```bash
# Inspect available services with reflection
grpcurl -plaintext localhost:50051 list

# cellaflow.v1.WorkflowEngineService
# cellaflow.v1.IdempotencyService
```

The Python SDK connects over cleartext by default (`secure=False`). For production, point to your engine with TLS enabled and pass your API key:

```python
@workflow(version="1.0.0", target="engine.internal:50051", secure=True)
def my_workflow(input: str) -> str:
    ...
```

Set `CELLAFLOW_AUTH_TOKEN` in the engine's environment, and the SDK reads it automatically from your local environment.

---

## Latency

Every number below was measured against `ghcr.io/cellaflow/cellaflow:latest`, driven by the Python
SDK's `CellaflowClient` over loopback gRPC, with a **single sequential writer** and a 136-byte
MessagePack payload. The host was a 2018 Intel Core i7-8850H MacBook Pro running Docker Desktop —
deliberately not fast hardware, so treat these as a floor rather than a best case. 400 samples per
figure after 50 warm-up calls.

| Operation | p50 | p95 | p99 |
|:---|---:|---:|---:|
| `CommitStep` — durable step commit | 1.6 ms | 2.3 ms | 2.8 ms |
| `GetGraph` — read a 3-step session | 1.6 ms | 2.3 ms | 3.1 ms |
| `CheckIdempotencyCache` — cache hit | 1.6 ms | 2.2 ms | 2.5 ms |
| MessagePack encode (in-process) | 2.0 µs | — | 5.2 µs |

### The write is not the cost

The three RPCs land within noise of each other, and `CommitStep` minus `GetGraph` — a durable write
minus a read that performs no write — came out at **0.02 ms** across two runs. Serialization is
three orders of magnitude below the RPC itself.

What you are paying for a durable step is therefore **one gRPC round trip**, not storage work. The
practical consequence: co-locating the engine with your agent (same host, or a sidecar) matters far
more for latency than anything about the storage layer, and moving it across a network boundary will
dominate every figure in the table above.

### These are single-writer numbers

Contention changes the picture completely. Workers committing to *one* session serialize behind that
session's actor, and because sequence ordering is enforced (`sequence == current + 1`), a writer that
loses the race refreshes its position and retries. Same host and payload as above, each writer
committing one step, released simultaneously:

| Writers on one session | p50 | p99 | Wall clock | Median attempts per writer |
|---:|---:|---:|---:|---:|
| 1 | 7 ms | 7 ms | 0.01 s | 1 |
| 5 | 20 ms | 34 ms | 0.04 s | 3 |
| 25 | 95 ms | 153 ms | 0.16 s | 7 |
| 50 | 278 ms | 459 ms | 0.47 s | 9 |
| 100 | 771 ms | 1298 ms | 1.35 s | 11 |

**Every writer lands at every level** — contention costs retries, not failures. Sessions are the
unit of concurrency: independent sessions never contend with one another, so a workload spread
across its own sessions scales flat while one shared session serialises behind its actor.

<Note>
Committed steps are written to RocksDB's write-ahead log, so a session survives a process crash, a
container restart or a pod eviction and resumes from its last durable step.
</Note>
